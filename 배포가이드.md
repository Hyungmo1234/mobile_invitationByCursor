# 도메인 배포 가이드

## 🚀 배포 방법 옵션

### 방법 1: Vercel (가장 추천 - Next.js 제작사)

**장점:**
- Next.js 최적화
- 무료 플랜 제공
- 자동 HTTPS
- 도메인 연결 쉬움
- Git 연동으로 자동 배포

**단계:**

1. **Vercel 계정 생성**
   - https://vercel.com 접속
   - GitHub 계정으로 로그인 (또는 이메일)

2. **프로젝트 업로드**
   - Vercel 대시보드에서 "Add New Project" 클릭
   - GitHub 저장소 연결 또는 직접 업로드
   - 프로젝트 폴더 선택

3. **배포 설정**
   - Framework Preset: Next.js (자동 감지)
   - Build Command: `npm run build` (자동)
   - Output Directory: `.next` (자동)
   - Install Command: `npm install` (자동)

4. **도메인 연결**
   - 프로젝트 설정 → Domains
   - 원하는 도메인 입력
   - DNS 설정 안내 따르기

5. **배포 완료!**
   - 자동으로 `https://your-domain.com` 생성

---

### 방법 2: Netlify

**단계:**

1. **Netlify 계정 생성**
   - https://www.netlify.com 접속

2. **프로젝트 배포**
   - "Add new site" → "Deploy manually"
   - 또는 GitHub 연동

3. **빌드 설정**
   - Build command: `npm run build`
   - Publish directory: `.next`

4. **도메인 연결**
   - Site settings → Domain management

---

### 방법 3: 자체 서버 (VPS)

**필요한 것:**
- VPS 서버 (예: AWS EC2, DigitalOcean, Linode 등)
- 도메인
- Node.js 설치

**단계:**

1. **서버에 Node.js 설치**
2. **프로젝트 업로드** (Git, FTP 등)
3. **프로덕션 빌드**
   ```bash
   npm run build
   npm start
   ```
4. **PM2로 프로세스 관리** (선택)
   ```bash
   npm install -g pm2
   pm2 start npm --name "wedding-invitation" -- start
   ```
5. **Nginx 설정** (리버스 프록시)
6. **도메인 DNS 설정**

---

### 방법 4: GitHub Pages (정적 사이트만)

Next.js를 정적 사이트로 내보내기:
```bash
# next.config.js에 추가
output: 'export'
```

---

## 📝 배포 전 체크리스트

### 1. 환경 변수 확인
- 필요한 API 키가 있다면 환경 변수로 설정

### 2. 이미지 최적화
- `public/images/` 폴더의 이미지 파일 확인
- 용량이 크면 최적화 권장

### 3. 빌드 테스트
```bash
npm run build
npm start
```
로컬에서 프로덕션 빌드 테스트

### 4. .gitignore 확인
- `node_modules/`, `.next/` 등이 제외되어 있는지 확인

---

## 🌐 도메인 구매 및 연결

### 도메인 구매 사이트
- 가비아 (한국)
- 후이즈 (한국)
- Namecheap (해외)
- GoDaddy (해외)

### DNS 설정
도메인 구매 후 DNS 설정:
- A 레코드: 서버 IP 주소
- CNAME: 서브도메인 (예: www)

---

## 💡 추천: Vercel 사용하기

**가장 간단한 방법:**

1. **GitHub에 코드 업로드**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin [GitHub 저장소 URL]
   git push -u origin main
   ```

2. **Vercel에서 GitHub 연동**
   - Vercel → New Project → GitHub 선택
   - 저장소 선택 → Deploy

3. **도메인 연결**
   - 프로젝트 → Settings → Domains
   - 도메인 입력 및 DNS 설정

**완료!** 몇 분 안에 배포 완료됩니다.

---

## 🔧 프로덕션 빌드 최적화

### next.config.js 확인
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // Vercel에서는 자동 최적화
  },
}
```

### package.json 스크립트
```json
{
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

---

## ❓ 문제 해결

### 빌드 에러
- 터미널에서 `npm run build` 실행하여 에러 확인
- TypeScript 에러 수정
- 의존성 문제: `npm install` 재실행

### 이미지 로드 안 됨
- `public/images/` 경로 확인
- 파일명 대소문자 확인

### 폰트 로드 안 됨
- `app/fonts/` 폴더에 파일이 있는지 확인
- 경로 확인

